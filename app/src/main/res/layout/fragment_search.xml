<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/surface_bg"
    android:padding="20dp">


    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/discover_eyebrow"
        android:textAppearance="@style/TextAppearance.EduFlex.Eyebrow"
        android:layout_marginBottom="8dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/nav_discover"
        android:textColor="@color/text_title"
        android:textSize="32sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="20dp"
        android:text="@string/discover_description"
        android:textColor="@color/text_secondary"
        android:textSize="16sp" />
    <!-- Search bar with sparkle icon -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginBottom="20dp">

        <EditText
            android:id="@+id/et_search"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_search_ai"
            android:textColor="@color/text_title"
            android:textColorHint="@color/text_secondary"
            android:inputType="textCapSentences"
            android:contentDescription="@string/search_courses_hint"
            android:singleLine="true"
            android:imeOptions="actionSearch"
            android:paddingStart="44dp"
            android:paddingEnd="52dp"
            android:textSize="15sp" />

        <!-- Animated hint overlay (sits on top of EditText, hidden when typing) -->
        <TextView
            android:id="@+id/tv_animated_hint"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:paddingStart="44dp"
            android:paddingEnd="52dp"
            android:importantForAccessibility="no"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="@color/text_secondary"
            android:textSize="15sp"
            android:clickable="false"
            android:focusable="false" />

        <!-- Sparkle icon -->
        <ImageView
            android:id="@+id/iv_sparkle"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="start|center_vertical"
            android:layout_marginStart="12dp"
            android:importantForAccessibility="no"
            android:src="@drawable/ic_sparkle" />

        <androidx.appcompat.widget.AppCompatImageButton
            android:id="@+id/btn_clear_search"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="end|center_vertical"
            android:padding="14dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_close"
            android:contentDescription="@string/clear_search"
            android:visibility="gone" />

    </FrameLayout>

    <!-- Shimmer loading placeholder -->
    <LinearLayout
        android:id="@+id/shimmer_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone">

        <View
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:layout_marginBottom="10dp"
            android:background="@drawable/bg_shimmer_item" />

        <View
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:layout_marginBottom="10dp"
            android:background="@drawable/bg_shimmer_item" />

        <View
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:layout_marginBottom="10dp"
            android:background="@drawable/bg_shimmer_item" />
    </LinearLayout>

    <TextView
        android:id="@+id/tv_search_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/search_no_results"
        android:textColor="@color/text_body"
        android:textSize="15sp"
        android:gravity="center"
        android:layout_marginTop="32dp"
        android:visibility="gone" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_search_retry"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="12dp"
        android:text="@string/search_retry"
        android:visibility="gone" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_search_results"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingBottom="12dp"
        android:visibility="gone" />

</LinearLayout>
